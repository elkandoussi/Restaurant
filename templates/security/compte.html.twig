{% extends "base.html.twig" %}

{% block header %}
<div class="row">
    {% include "layouts/header.html.twig" %}
</div>
{% endblock %}

{% block body %}
<div class="row">
    <div class="col-12">
        <h3 class="">Bievenue, {{ client.firstname }} </h3>
    </div>
</div>

<div class="row">
    <div class="col-12">
        <h5 class="">Informations Générale:</h5>
        <hr>
    </div>
</div>

<div class="row" id="test">
    <informations firstname="{{client.firstname}}" lastname="{{client.lastname}}"
        email ="{{client.email}}" password ="{{ client.password }}">
    </informations>
</div>
{% endblock %}